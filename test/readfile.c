#include <iostream>#include <conio.h>#include <fstream>#include <string>using namespace std;#define rank_max 5void main(){ FILE* fp; char buff[20]; int score[rank_max];  int count=0; int i; ifstream file; file.open("score.txt"); fopen_s(&fp, "score.txt", "r");  //읽기모드 if (fp == NULL)   //정상적으로 파일을 읽었는지 검사 {  cout << "파일 개방 실패" << endl;  return; }  while (!file.is_open()) //한줄씩 읽어내려감 {  fgets(buff, sizeof(buff), fp);  char* Data;  Data = strtok(buff, " ");  while (Data = strtok(NULL, " "))   {    cout << count+1 << "등 " ;    score[count] = atoi(Data);    cout.width(6);    cout << buff<<"  ";   }  cout.width(7);  cout << score[count] << endl;  count++; } file.close(); //return(score[count]);  }
